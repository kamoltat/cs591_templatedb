#include "lsm.h"

using namespace std;
// using namespace templatedb;

int main () {
    // Say HelloWorld five times
    // cout << "here";
    size_t n = 500;
    lsm* temp;
    temp = init_new_lsm(n);

    return 0;
}